#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Point of entry
 *
 * Return: 0 if successeful
 */
int main(void)
{
	char low;

	for (d = '0'; d <= '9'; d++)
	{
	putchar(low);
	}
	putchar('\n');
	return (0);
}	
